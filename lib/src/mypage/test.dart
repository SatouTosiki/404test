import 'dart:html';

import 'package:flutter/material.dart';
import 'mypage.dart';

class test2 extends StatelessWidget {
  const test2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Row(children: [Text("adffdf")]),
    );
  }
}
